* Ctrl+D doet een Soft Reset. Dan zul je eerst het resultaat van je code zien en krijg je daarna de interactieve shell (wanneer je programma is afgelopen).
* Ctrl+C voor break naar de interactieve REPL shell.

```
while True:
    print("Hoi")
```
